# Evolutionary routing entrypoint

Read in this order:

1. `ECOLOGICAL_STATE_OBSERVABILITY.md`
2. `EVOLUTIONARY_INFORMATION_ROUTING.md`
3. `TEMPORAL_ROUTING_THRESHOLD.md`
4. `NOISY_TEMPORAL_ROUTING_FACTORIZATION.md`
5. `PRIOR_ART_EVOLUTIONARY_ROUTING.md`
6. `NEXT_NOISY_ROUTING_MODEL.md`

The first exact dynamic result is

\[
A_F^{(2)}=3/4,
\qquad
A_A^{(2)}=3/4+|2\rho-1|/4,
\qquad
G_{time}=|2\rho-1|/4.
\]

With symmetric binary observation noise,

\[
\boxed{
G_{noisy}
=
\frac{|2\rho-1|(2a-1)(2b-1)}{4}.
}
\]

The conceptual result is therefore stronger than `persistent environments favour routing`:

```text
temporal predictability
x usable early routing information
x usable downstream specialist information
```

jointly determine the value of contingent sensing in the minimal model. Predictable alternation reverses the continuation rule rather than destroying routing value.
